export { type MemoryKind, type MemoryRecord } from '../../shared/src/index.js';

// Retrieval is local and agent-scoped. The persistence repository ranks keyword
// overlap, salience, and recency and consolidates consecutive duplicates.
